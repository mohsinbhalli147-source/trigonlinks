rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client-side access by default
    // All access should go through the backend API using Firebase Admin SDK
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
